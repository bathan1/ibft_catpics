{
    "name": "ibft_catpics",
    "version": "1.0.0",
    "packageManager": "pnpm@9.1.1",
    "description": "lol",
    "engines": {
        "node": "20.x"
    },
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "cd binding && npm install && npx node-gyp configure && npx node-gyp build && cd ../client && npm install && npm build",
        "start": "node app.js"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "dependencies": {
        "@hono/node-server": "^1.17.1",
        "@hono/zod-validator": "^0.7.2",
        "hono": "^4.8.9",
        "zod": "^4.0.10"
    }
}
